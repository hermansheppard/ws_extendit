/* globals Shared */

(function() {

  survey_app.survey_appC = Trillo.inherits(Shared.AppC, function(viewSpec) {
    Shared.AppC.call(this, viewSpec);
  });

  var survey_appC = survey_app.survey_appC.prototype;
  var AppC = Shared.AppC.prototype;
})();
